/// @func setWallDialog()
/// @description set the dialog text for walls

dialogText = (
	name + "\n" +
	"\nLevel: " + string(level) +
	"\nHealth: " + string(currentHealth) + 
	"\nStatus: " + status
	)